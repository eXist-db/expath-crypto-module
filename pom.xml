<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.exist-db</groupId>
        <artifactId>exist-apps-parent</artifactId>
        <version>2.0.0</version>
        <relativePath/>
    </parent>

    <groupId>org.exist-db</groupId>
    <artifactId>exist-crypto</artifactId>
    <version>7.0.0-SNAPSHOT</version>

    <name>EXPath Crypto Module</name>
    <description>EXPath Cryptographic Module for eXist-db: HMAC, symmetric encryption/decryption, and XML digital signatures using Java JCE</description>
    <url>https://github.com/eXist-db/exist-crypto</url>

    <organization>
        <name>The eXist-db Authors</name>
        <url>https://exist-db.org</url>
    </organization>

    <licenses>
        <license>
            <name>GNU Lesser General Public License, version 2.1</name>
            <url>https://opensource.org/licenses/LGPL-2.1</url>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/eXist-db/exist-crypto.git</url>
        <connection>scm:git:https://github.com/eXist-db/exist-crypto.git</connection>
        <developerConnection>scm:git:https://github.com/eXist-db/exist-crypto.git</developerConnection>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.source>21</project.build.source>
        <project.build.target>21</project.build.target>

        <exist.version>7.0.0-SNAPSHOT</exist.version>

        <!-- used in the EXPath Package Descriptor -->
        <package-name>http://expath.org/ns/crypto</package-name>
        <package-abbrev>exist-crypto</package-abbrev>

        <crypto.module.namespace>http://expath.org/ns/crypto</crypto.module.namespace>
        <crypto.module.java.classname>org.exist.xquery.modules.crypto.CryptoModule</crypto.module.java.classname>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.exist-db</groupId>
            <artifactId>exist-core</artifactId>
            <version>${exist.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.sf.xmldb-org</groupId>
            <artifactId>xmldb-api</artifactId>
            <version>2.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>integration-tests</id>
            <dependencies>
                <!-- Override provided scope so transitive deps (Saxon, etc.) are on test classpath -->
                <dependency>
                    <groupId>org.exist-db</groupId>
                    <artifactId>exist-core</artifactId>
                    <version>${exist.version}</version>
                    <scope>compile</scope>
                </dependency>
                <dependency>
                    <groupId>org.exist-db</groupId>
                    <artifactId>exist-start</artifactId>
                    <version>${exist.version}</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                    <version>4.13.2</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>ro.kuberam.maven.plugins</groupId>
                <artifactId>kuberam-expath-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <!-- exist-start is needed at runtime for the embedded test server
                         but not directly referenced in code -->
                    <ignoredUnusedDeclaredDependencies>
                        <ignoredUnusedDeclaredDependency>org.exist-db:exist-start</ignoredUnusedDeclaredDependency>
                    </ignoredUnusedDeclaredDependencies>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>exist-db</id>
            <name>eXist-db Releases</name>
            <url>https://repo.exist-db.org/repository/exist-db/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>exist-db-snapshots</id>
            <name>eXist-db Snapshots</name>
            <url>https://repo.exist-db.org/repository/exist-db-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
</project>
